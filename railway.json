{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate && cd backend && npx esbuild src/index.ts --bundle --platform=node --target=node20 --outdir=dist --format=cjs --sourcemap --external:@prisma/client --external:better-sqlite3 --external:geoip-lite"
  },
  "deploy": {
    "startCommand": "node backend/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
